# Web Calculator

The user interface for the Service Generator. Currently a static dashboard showing backend status and platform information.

This UI demonstrates the frontend component of a Golden Path service — simple, production-ready, and automatically deployed alongside the backend via the same GitOps pipeline.
